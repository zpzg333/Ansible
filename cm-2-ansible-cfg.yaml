apiVersion: v1
kind: ConfigMap
metadata:
  name: ansible.cfg
  namespace: ansible
data:
  ansible.cfg: |-
    [defaults]
    inventory = ./inventory/hosts.ini
    host_key_checking = False
    interpreter_python = /usr/local/bin/python3.12
    collections_path = /root/.ansible/collections
